<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Empowering Your Finances</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .hero {
            background-image: url("path/to/financial-inclusion-image.jpg");
            background-size: cover;
            height: 400px;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .hero h1 {
            color: #fff;
            font-size: 2em;
            text-align: center;
        }
        .buttons {
            display: flex;
            justify-content: space-between;
            margin-top: 20px;
        }
        button {
            padding: 10px 20px;
            border: 1px solid #ddd;
            background-color: #eee;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="hero">
        <h1>Financial Inclusion Starts Here</h1>
    </div>
    <div class="buttons">
        <button>Learn More</button>
        <button>Create Account</button>
    </div>
</body>
</html>
